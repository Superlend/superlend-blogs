"use client";

/**
 * This component ensures light theme is always used.
 * Since we're using light mode only, no client-side script is needed.
 */
export const ThemeSwitcher = () => {
  return null;
};
